<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.kevin.oasis.dao.JobAlarmPolicyDao">

    <resultMap id="BaseResultMap" type="com.github.kevin.oasis.models.entity.JobAlarmPolicy">
        <id column="id" property="id"/>
        <result column="job_id" property="jobId"/>
        <result column="inherit_app_template" property="inheritAppTemplate"/>
        <result column="receivers" property="receivers"/>
        <result column="channels" property="channels"/>
        <result column="quiet_period_minutes" property="quietPeriodMinutes"/>
        <result column="fail_threshold" property="failThreshold"/>
        <result column="timeout_seconds" property="timeoutSeconds"/>
        <result column="enabled" property="enabled"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByJobId" resultMap="BaseResultMap">
        SELECT id, job_id, inherit_app_template, receivers, channels, quiet_period_minutes, fail_threshold, timeout_seconds, enabled, create_time, update_time
        FROM job_alarm_policy
        WHERE job_id = #{jobId}
    </select>

    <insert id="insert" parameterType="com.github.kevin.oasis.models.entity.JobAlarmPolicy">
        INSERT INTO job_alarm_policy (
            job_id, inherit_app_template, receivers, channels, quiet_period_minutes, fail_threshold, timeout_seconds, enabled, create_time, update_time
        ) VALUES (
            #{jobId}, #{inheritAppTemplate}, #{receivers}, #{channels}, #{quietPeriodMinutes}, #{failThreshold}, #{timeoutSeconds}, #{enabled}, NOW(), NOW()
        )
    </insert>

    <update id="updateByJobId" parameterType="com.github.kevin.oasis.models.entity.JobAlarmPolicy">
        UPDATE job_alarm_policy
        SET inherit_app_template = #{inheritAppTemplate},
            receivers = #{receivers},
            channels = #{channels},
            quiet_period_minutes = #{quietPeriodMinutes},
            fail_threshold = #{failThreshold},
            timeout_seconds = #{timeoutSeconds},
            enabled = #{enabled},
            update_time = NOW()
        WHERE job_id = #{jobId}
    </update>

</mapper>
