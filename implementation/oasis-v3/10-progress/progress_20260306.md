# progress_20260306

## 今日完成
1. 完成调度执行闭环改造（Admin）：
   - 新增调度扫描器 `ScheduleScannerService`，周期扫描 `job_schedule` 到期任务。
   - 新增 `job_schedule` CAS 抢占更新，避免多 Admin 节点重复触发。
   - 新增执行器离线摘除任务，超时心跳节点自动置 `OFFLINE`。
2. 完成执行器路由下发实现：
   - `ROUND`、`RANDOM`、`FAILOVER`、`BROADCAST` 路由策略落地。
   - 触发链路从“仅写日志”升级为“写日志 -> 下发 -> 更新状态”。
3. 完成 starter 稳定性增强：
   - Admin 回包按 `code == 0000` 判成功，避免业务失败误判。
   - `RestTemplate` 连接/读取超时配置生效。
   - 修复 JDK17 兼容问题（`Thread#threadId` -> `Thread#getId`）。
4. 完成前端与 i18n 修正：
   - 任务编辑抽屉支持任务级告警策略加载与保存。
   - 调度日志页新增详情抽屉。
   - 修复路由 i18n key：`schedule_job_alarm-events`。
5. 完成执行器安全闭环与接收端架构调整：
   - Admin 增加执行器接口 HMAC 验签（时间窗、nonce、防重放、签名）。
   - 回调链路新增 `fireLog` 归属校验。
   - starter 去除 Controller 接收，改为内置 Netty HTTP Server 接收 `/invoke`。

## 编译与检查结果
- `mvn -q -f oasis-admin/pom.xml -DskipTests compile` 通过。
- `mvn -q -f oasis-scheduler-spring-boot-starter/pom.xml -DskipTests compile` 通过。
- `pnpm -C oasis-web typecheck` 未全量通过，仅剩既有问题 2 项：
  - `src/views/application/modules/application-search.vue`
  - `src/views/manage/user/modules/user-search.vue`

## 风险与待办
- 自动调度当前实现为“数据库扫描 + CAS 抢占”，尚未接入 `shard_lease` 分片租约优化。
- `dispatch_queue` 重试队列仍为预留表，尚未接入异步重试任务。
- `BROADCAST` 当前为安全降级（单节点执行），完整多节点广播需升级为“每节点独立 fireLog”模型。
- HMAC 签名当前仅 starter 侧发送，Admin 侧校验可在下一阶段补齐。

## 下一步
- 接入 `dispatch_queue` 异步重试与失败补偿。
- 增加调度与下发链路的集成测试/故障演练脚本。
- 评估并实现二期 OpenAPI（任务创建/修改/删除）能力预留。
