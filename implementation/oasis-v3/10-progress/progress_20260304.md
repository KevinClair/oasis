# progress_20260304

## 今日完成
1. 创建 `implementation/oasis-v3/` 独立归档目录。
2. 建立 00-10 全量子目录。
3. 新增 V3 实施计划、产品与技术架构文档。
4. 新增 SQL 基线脚本与回滚脚本（无前缀表名）。
5. 新增 API、starter、前后端、测试、发布模板文档。

## 当前风险
- SQL 仅为设计基线，未与实际代码实体完成映射校验。
- OpenAPI 与长连接属于二期内容，暂未落地实现。

## 下一步
- 基于该目录按阶段推进代码实现与联调。
- 每次接口/配置/页面变更按规范更新对应文档。

## 今日增量（前后端首批逻辑）
1. 后端新增 `ScheduleController` 与 `ExecutorController`，完成任务、日志、告警模板/策略/事件、执行器注册心跳回调接口初版。
2. 后端新增调度域 DAO/Entity/VO/Service 与 MyBatis Mapper：
   - `job_info/job_schedule/job_fire_log/job_log_chunk`
   - `app_alarm_template/job_alarm_policy/job_alarm_event/job_alarm_notify_record`
   - `executor_node`
3. 新增 SQL 脚本：
   - `oasis-admin/src/main/resources/sql/create_schedule_tables_v3.sql`
   - `oasis-admin/src/main/resources/sql/init_schedule_menu.sql`
4. 前端新增页面：
   - `/schedule/job`
   - `/schedule/job/alarm-events/:jobId`
   - `/schedule/log`
5. 前端扩展 API 与类型：
   - `src/service/api/system-manage.ts` 新增 `schedule/*` 接口封装
   - `src/typings/api/system-manage.d.ts` 新增调度与告警类型
6. 应用管理新增“告警模板”弹窗入口，实现应用默认告警模板编辑。
7. 动态路由手工补齐：`imports.ts`、`transform.ts`、`routes.ts`、`elegant-router.d.ts`。

## 阻塞/说明
- `oasis-admin` 无法本地编译校验：当前 Maven 版本低于 `maven-compiler-plugin 3.13.0` 要求。
- `oasis-web` `typecheck` 仍有既有报错（非本次改动引入）：
  - `src/views/application/modules/application-search.vue`
  - `src/views/manage/user/modules/user-search.vue`
